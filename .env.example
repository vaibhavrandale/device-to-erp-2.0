# Same MongoDB the taypro-console-backend uses (HRUser + AttendancePunch live there)
MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/taypro

# Local broker on the Pi (mosquitto). Leave as-is.
MQTT_URL=mqtt://127.0.0.1:1883
